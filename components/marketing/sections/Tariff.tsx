'use client'

import Reveal from '@/components/marketing/motion/Reveal'

export default function TariffSection() {
  return (
    <section data-surface="light" className="mk-section">
      <div className="mk-container">
        <Reveal>
          <h2 className="mk-h2 max-w-[22ch]">Duty on the same line as stock.</h2>
          <p className="mk-lead mt-6 max-w-[52ch] text-mk-ink-muted">
            If a part is cheaper by eleven cents and carries 25 points of Section 301, the landed
            cost went up. That number sits next to lifecycle and stock, in the same buy call.
          </p>
        </Reveal>
      </div>
    </section>
  )
}
